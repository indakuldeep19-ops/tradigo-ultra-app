<?xml version="1.0" encoding="utf-8"?>
<<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.tradigoultra.com</domain>
        <pin-set>
            <!-- Replace with real SHA-256 pins from local.properties via BuildConfig -->
            <!-- openssl s_client -connect api.tradigoultra.com:443 </dev/null | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | base64 -->
        </pin-set>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
